<?xml version="1.0" encoding="utf-8"?>
<!-- Native launch screen, shown before the Flutter engine draws its first
     frame. Kept visually identical to the in-app splash (white mark on
     black) so startup has no colour flash. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/taskaya_splash_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher_foreground" />
    </item>
</layer-list>
